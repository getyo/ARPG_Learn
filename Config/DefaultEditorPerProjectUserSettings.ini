[/Script/ModelContextProtocolEngine.ModelContextProtocolSettings]
; Unreal MCP (Epic's official Model Context Protocol server plugin)
; Serves an MCP endpoint at http://localhost:<port><path> for external AI agents.
ServerUrlPath=/mcp
ServerPortNumber=8000
bAutoStartServer=True
; When true, tools/list only exposes list_toolsets / describe_toolset / call_tool
; (the LLM discovers toolsets on demand instead of registering every tool up front).
bEnableToolSearch=True

[/Script/MCPClientToolset.MCPToolsetSettings]
; Add rows here to let the in-editor AI Assistant consume external MCP servers.
MCPServers=

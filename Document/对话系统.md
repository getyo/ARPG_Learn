# 对话系统

## 一、类图

```mermaid
classDiagram 
	 DialogueLine *-- DialogueDataAsset
	 FInQuestTargetDialogMap *-- DialogueSubsystem
	 DialogueDataAsset *-- DialogueSubsystem
	 DialogueSubsystem o-- DialogueComponent 
	 DialogueSubsystem --o QuestSubSystem
	 class DialogueLine{
		+ FText DialogueText;
		+ FGameplayTag SpeakerName;
	 }
	 class DialogueDataAsset{
		+ TArray<FDialogueLine> DialogueLines;
		+ FGameplayTag RelativeQuestID;
		+ int Stage;
	 }
	 class FInQuestTargetDialogMap{
	 	+ TMap ~int,UDialogueDataAsset*~ Map;
	 }
	 class DialogueSubsystem{
	 	- TMap ~FGameplayTag,FInQuestTargetDialogMap~ DialogueAssetMap;
	 	- FGameplayTag CurrentQuest;
		- int CurrentStage;
		- size_t TextIt;
		- AThirdPersonPlayerController* CurrentPlayerController;
		- UDialogueDataAsset * CurrentDialogueAsset;
		+ bool StartDialogue(const FGameplayTag &QuestID, int Stage, AController* InstigatorControllerer)
		+ bool EndDialogue()
		+ FDialogueLine NextDialogue()
		+ bool IsStageDialogueEnd() const
	 }
	 class DialogueComponent {
		# UDialogueSubsystem* DialogueSubsystem;
		# bool InDialogue;
		+ void SetInDialogue(bool Value)
		+ bool IsInDialogue() const
		+ StartDialogue(const FGameplayTag &QuestID,const int Stage,AController* InstigatorController)
		+ void EndDialogue()
	 }
```

解释如下：

1. 真正的数据驱动实现在对话资产里面，编辑器里面也是操作对话资产
2. 对话组件本身是对对话子系统的封装，为了让单个角色对话启动时逻辑更清晰。
3. 对话组件不能有下一句这个功能，因为对话系统本身是集中式的设计，下一句的Speaker不定是组件的Owner，他获取了属于其他人的对话逻辑上不正确。
4. 任务子系统唯一的耦合点在于广播对话完成这一任务目标完成条件

## 二、对话系统初始化以及对话资产读取入流程

```mermaid
flowchart
Start(开始)-->Edit[在编辑器中创建并完成对话资产的编辑]
Edit-->ReadDialogue[DialogueSubSystem读入所有对话资产]
ReadDialogue-->Classify[对所有对话资产按照任务，阶段添加到DialogueAssetMap]
Classify-->End[结束]
```

## 三、对话进行流程

```mermaid
flowchart 

Start(开始)-->PlayInteract[玩家与角色互动开始对话]
PlayInteract-->DialogueStart[某个角色的DialogueComponent调用StartDialogue]
DialogueStart-->DialogueSysSt[上述调用转交给DialogueSubSystem并调用同名函数]
DialogueSysSt-->FindDialogue[DialogueSubSystem根据任务，阶段寻找对话资产]
FindDialogue --> HasDialogue?{是否存在所需资产？}
HasDialogue?--是-->DialogueSysSet[对话系统设置当前对话资产相关数据，准备逐句返回对话]
HasDialogue?--否-->End(结束并报错)
DialogueSysSet-->ShowDialogue[UI界面相关逻辑直接与DialogueSubSystem交互逐句显示对话]
ShowDialogue-->PlayerInteractContinue[玩家继续点击互动按键]
PlayerInteractContinue --> DialogueEnd?{对话是否结束？}
DialogueEnd? --是--> EndDialogueTagSet[UI界面设置相关对话结束标签]
DialogueEnd? --否--> ShowDialogue
EndDialogueTagSet --> EndDialogue[开启对话角色检测到对话结束，DialogueComponent调用EndDialogue]
EndDialogue --> DialogueSysEnd[上述调用转交给DialogueSubsystem，并调用同名函数]
DialogueSysEnd --> IsDaily{是否任务相关对话，即判断对话资产的任务ID是否为空（空为非任务对话）}
IsDaily --否--> BoradcastFinish[广播对话完成目标条件（用于任务目标完成）]
IsDaily --是--> ClearCurDialogue[DIalogueSubsystem将所有有关当前对话的内容置空，防止出错]
BoradcastFinish--> ClearCurDialogue
ClearCurDialogue --> End(结束)

```

## 四、如何添加和编辑一个对话资产？并让其在任务中生效？

### 4.1  创建对话资产

1. 去到`Content\Dialogue\Asset`目录下，右键`其他->数据资产->DialogueDataAsset`，建议的命名格式：`QuestNameStage_DialogueName`

2. 打开创建好的数据资产，界面如下：![image-20260209093744766](D:\UnrealProject\ARPG_Learn\Document\image-20260209093744766.png)

3. 在`DialogueLine`中可以添加对话，每个对话都要选择`Speaker`并且添加对话内容。
4. 选择`RelatiiveQuestTag`以及`Stage`，前者是对话所属的任务，后者是对话在任务内的哪个阶段，若`RelativeQuestTag`为空，那就是日常对话，`Stage`不管填什么都不生效。

### 4.2 关联到相应NPC

1. 到你要对话的NPC的蓝图编辑器中，找到`Quest`标签下的`RelativeQuestMap`变量。

<img src="image-20260209185252320.png" alt="image-20260209185252320" style="zoom:50%;" />

2. 添加元素，说明：

   - 键是任务Tag
   - 值是该NPC与任务的哪个阶段关联，由于可能不止一个阶段，所以是一个数组。
   - 数组的元素填写任务用于确定阶段的`StageInt`以及完成条件中的`ActionTag`，`StageInt`参考任务数据表格填入你想要的阶段，`ActionTalk`这里选择`Talk`

   <img src="image-20260209185334397.png" alt="image-20260209185334397" style="zoom: 67%;" />

3. 还记得第一次我还圈住了一个`Current Foucs Quest ID`吗？这个变量如果不设置是NPC不会进行任何任务相关操作的。

4. 完成上述编辑后，当你每一次完成相关对话都会自动推进`Foucs Quest`的阶段。

















